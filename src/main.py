





def main():
    # node = TextNode("This is **text** with an _italic_ word and a `code block` and an ![obi wan image](https://i.imgur.com/fJRm4Vk.jpeg) and a [link](https://boot.dev)", TextType.TEXT)
    # bold = split_nodes_delimiter([node], "**", TextType.BOLD)
    # italics = split_nodes_delimiter(bold, "_", TextType.ITALIC)
    # code = split_nodes_delimiter(italics, "`", TextType.CODE)
    # image = split_nodes_image(code)
    # link = split_nodes_link(image)

    # print(link)
    pass

   

main()